import 'package:flutter/material.dart';

class HealthAdviceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pet health advice'),
        backgroundColor: Color(0xFF32A287),
      ),
    );
  }
}
